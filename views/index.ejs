<!DOCTYPE html>
<html>
    <head>
        
    </head>
    <body>
        <% if (!isAuthenticated) { %>
            <a href="/login">Log in here</a>
        <% } else {  %>
            Hello, <%= user.name %>
            <a href="/logout">Log Out</a>
        <% } %>
    </body>
</html>